---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: hypervisormachinetemplates.hypervisor.hyperfleet.io
spec:
  group: hypervisor.hyperfleet.io
  names:
    kind: HypervisorMachineTemplate
    listKind: HypervisorMachineTemplateList
    plural: hypervisormachinetemplates
    singular: hypervisormachinetemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.template.proxmox.templateId
      name: Template ID
      type: integer
    - jsonPath: .spec.resources.cpu
      name: CPU
      type: integer
    - jsonPath: .spec.resources.memory
      name: Memory
      type: string
    - jsonPath: .status.templateAvailable
      name: Available
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HypervisorMachineTemplate is the Schema for the hypervisormachinetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HypervisorMachineTemplateSpec defines the desired state of
              HypervisorMachineTemplate.
            properties:
              attestation:
                description: Attestation configures VM identity verification
                properties:
                  config:
                    description: Config provides method-specific configuration
                    properties:
                      joinTokenTTL:
                        description: JoinTokenTTL specifies join token time-to-live
                        type: string
                      tpmDevice:
                        description: TPMDevice specifies TPM device path for TPM attestation
                        type: string
                    type: object
                  method:
                    description: Method specifies the attestation method (join-token,
                      tpm)
                    enum:
                    - join-token
                    - tpm
                    type: string
                required:
                - method
                type: object
              bootstrap:
                description: Bootstrap configures workload credential provisioning
                properties:
                  config:
                    description: Config provides method-specific configuration
                    properties:
                      github:
                        description: GitHub configuration for runner-token method
                        properties:
                          app:
                            description: App configuration for GitHub App credentials
                              (recommended)
                            properties:
                              appId:
                                description: AppID references the GitHub App ID
                                properties:
                                  key:
                                    description: Key within the secret
                                    type: string
                                  name:
                                    description: Name of the secret
                                    type: string
                                  namespace:
                                    description: Namespace of the secret, defaults
                                      to the same namespace as the referring object
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              installationId:
                                description: InstallationID references the GitHub
                                  App installation ID
                                properties:
                                  key:
                                    description: Key within the secret
                                    type: string
                                  name:
                                    description: Name of the secret
                                    type: string
                                  namespace:
                                    description: Namespace of the secret, defaults
                                      to the same namespace as the referring object
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                              privateKey:
                                description: PrivateKey references the GitHub App
                                  private key
                                properties:
                                  key:
                                    description: Key within the secret
                                    type: string
                                  name:
                                    description: Name of the secret
                                    type: string
                                  namespace:
                                    description: Namespace of the secret, defaults
                                      to the same namespace as the referring object
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - appId
                            - installationId
                            - privateKey
                            type: object
                          pat:
                            description: PAT configuration for Personal Access Token
                              (development)
                            properties:
                              key:
                                description: Key within the secret
                                type: string
                              name:
                                description: Name of the secret
                                type: string
                              namespace:
                                description: Namespace of the secret, defaults to
                                  the same namespace as the referring object
                                type: string
                            required:
                            - key
                            - name
                            type: object
                          runner:
                            description: Runner configuration
                            properties:
                              downloadUrl:
                                description: DownloadURL for GitHub Actions runner
                                  binary
                                type: string
                              installPath:
                                description: InstallPath for runner installation
                                type: string
                              labels:
                                description: Labels for runner registration
                                items:
                                  type: string
                                type: array
                              workDir:
                                description: WorkDir for runner working directory
                                type: string
                            type: object
                          url:
                            description: Repository or organization URL for runner
                              registration
                            type: string
                        required:
                        - url
                        type: object
                    type: object
                  method:
                    description: Method specifies the bootstrap method (runner-token,
                      external-secrets)
                    enum:
                    - runner-token
                    - external-secrets
                    type: string
                required:
                - method
                type: object
              cloudInit:
                description: CloudInit provides custom cloud-init configuration
                properties:
                  metaData:
                    description: MetaData provides cloud-init meta data
                    type: string
                  userData:
                    description: UserData provides cloud-init user data
                    type: string
                type: object
              hypervisorClusterRef:
                description: HypervisorClusterRef references the target hypervisor
                  cluster
                properties:
                  name:
                    description: Name of the referent
                    type: string
                  namespace:
                    description: Namespace of the referent, defaults to the same namespace
                      as the referring object
                    type: string
                required:
                - name
                type: object
              network:
                description: Network defines network configuration for VMs
                properties:
                  dhcp:
                    description: DHCP provides DHCP-specific configuration
                    properties:
                      clientIdentifier:
                        description: ClientIdentifier specifies DHCP client identifier
                        type: string
                      requestStaticLease:
                        description: RequestStaticLease requests same IP for hostname
                        type: boolean
                      sendHostname:
                        description: SendHostname enables sending hostname in DHCP
                          request
                        type: boolean
                    type: object
                  mode:
                    description: Mode specifies network configuration mode (dhcp,
                      static, cloud-init)
                    type: string
                  staticConfig:
                    description: StaticConfig provides static IP configuration
                    properties:
                      dns:
                        description: DNS servers
                        items:
                          type: string
                        type: array
                      gateway:
                        description: Gateway IP address
                        type: string
                      ip:
                        description: IP address with CIDR notation
                        type: string
                    required:
                    - gateway
                    - ip
                    type: object
                type: object
              resources:
                description: Resources defines the VM resource requirements
                properties:
                  cpu:
                    description: CPU cores for the VM
                    maximum: 64
                    minimum: 1
                    type: integer
                  disk:
                    description: Disk size for the VM (e.g., "50G", "100G")
                    pattern: ^[0-9]+G$
                    type: string
                  memory:
                    description: Memory allocation for the VM (e.g., "4Gi", "8192Mi")
                    pattern: ^[0-9]+[KMGT]i?$
                    type: string
                required:
                - cpu
                - disk
                - memory
                type: object
              template:
                description: Template defines hypervisor-specific template configuration
                properties:
                  proxmox:
                    description: Proxmox-specific template configuration
                    properties:
                      clone:
                        description: Clone enables VM cloning from template
                        type: boolean
                      linkedClone:
                        description: LinkedClone enables linked clone for faster provisioning
                        type: boolean
                      templateId:
                        description: TemplateID is the Proxmox template ID to clone
                          from
                        type: integer
                    required:
                    - templateId
                    type: object
                type: object
            required:
            - attestation
            - bootstrap
            - hypervisorClusterRef
            - resources
            - template
            type: object
          status:
            description: HypervisorMachineTemplateStatus defines the observed state
              of HypervisorMachineTemplate.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastValidated:
                description: LastValidated timestamp of last validation
                format: date-time
                type: string
              templateAvailable:
                description: TemplateAvailable indicates if the referenced template
                  exists
                type: boolean
              validationStatus:
                description: ValidationStatus indicates template validation result
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
